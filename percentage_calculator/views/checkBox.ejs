<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Generated Checkboxes</title>
    <link rel="stylesheet" href="/style.css" />
    <style>
      /* ===== Global Styles ===== */
      body {
        font-family: Arial, Helvetica, sans-serif;
        background-color: #f9f9f9;
        color: #333;
        margin: 0;
        padding: 20px;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
      }

      .container {
        max-width: 500px;
        width: 100%;
        background: #fff;
        padding: 25px 20px;
        border-radius: 12px;
        box-shadow: 0 2px 12px rgba(0, 0, 0, 0.1);
      }

      h1 {
        color: #009342;
        text-align: center;
        margin-bottom: 25px;
      }

      .form-group {
        margin-bottom: 20px;
      }

      .form-group label {
        display: block;
        margin-bottom: 6px;
        font-size: 0.95rem;
        color: #555;
      }

      .form-group input[type='number'] {
        width: 100%;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 8px;
        font-size: 1rem;
        outline: none;
        transition: border-color 0.3s ease;
      }

      .form-group input[type='number']:focus {
        border-color: #009342;
      }

      button {
        width: 100%;
        padding: 12px;
        background-color: #009342;
        border: none;
        border-radius: 8px;
        color: #fff;
        font-size: 1rem;
        font-weight: bold;
        cursor: pointer;
        transition: background-color 0.3s ease;
      }

      button:hover {
        background-color: #007b38;
      }

      .back-link {
        display: inline-block;
        margin-top: 20px;
        text-align: center;
        color: #009342;
        text-decoration: none;
        font-weight: bold;
        font-size: 0.95rem;
      }

      .back-link:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Enter Marks for <%= count %> Subjects</h1>

      <form action="/result" method="POST">
        <% for (let i = 1; i <= count; i++) { %>
        <div class="form-group">
          <label for="obtainedMarks<%= i %>">
            Obtained Marks for Subject <%= i %>
          </label>
          <input
            type="number"
            id="obtainedMarks<%= i %>"
            name="obtainedMarks<%= i %>"
            min="0"
            placeholder="Enter obtained marks"
            required
          />
        </div>

        <div class="form-group">
          <label for="totalMarks<%= i %>">
            Total Marks for Subject <%= i %>
          </label>
          <input
            type="number"
            id="totalMarks<%= i %>"
            name="totalMarks<%= i %>"
            min="1"
            placeholder="Enter total marks"
            required
          />
        </div>
        <% } %>

        <!-- Hidden input to keep track of total subjects -->
        <input type="hidden" name="subjectCount" value="<%= count %>" />

        <button type="submit">Calculate Result</button>
      </form>

      <a href="/" class="back-link">← Back to Home Page</a>
    </div>
  </body>
</html>
